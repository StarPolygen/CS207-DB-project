<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cs307.sustc.project.dao.GoodDao">
    <select id="queryAllGoods" resultType="com.cs307.sustc.project.entity.Good">
        select * from good
    </select>
    <select id="queryGoodsByNumber" resultType="com.cs307.sustc.project.entity.Good">
        select * from good
        where release_time &lt; #{time}
        order by id desc
        limit 0, #{number};
    </select>
    <select id="queryGoodByID" resultType="com.cs307.sustc.project.entity.Good">
        select * from good
        where id = #{id};
    </select>
    <insert id="insertGood" parameterType="com.cs307.sustc.project.entity.Good">
       insert into good(seller_id, good_tag, `name`, description, good_status, price, last_price)
       values (#{seller_id}, #{good_tag}, #{name}, #{description}, #{good_status}, #{price}, #{last_price});
    </insert>
</mapper>